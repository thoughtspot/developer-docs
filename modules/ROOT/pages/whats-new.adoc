= What's new
:toc: true
:toclevels: 1

:page-title: What's new
:page-pageid: whats-new
:page-description: New features and enhancements

This page lists new features, enhancements, and deprecated functionality introduced for ThoughtSpot Embedded instances in 2026. For information about the features introduced in the previous release versions, see xref:whats-new-prev-history.adoc[release history].

// ============================================================
// VERSION TEMPLATE — copy this block for each new release
// ============================================================
//
//
// [.version-badge.breaking]#Breaking# /
// [.version-badge.new]#New# /
// [.version-badge.deprecated]#Deprecated# /
// [.version-badge.fixed]#Fixed#
//
// *API version string:* `?api-version=YYYY-MM-DD`
// *Status:* Current / Supported / Deprecated
// *Affects:* Developers, Administrators, End Users
// ============================================================

== August 2026

**Release version**: ThoughtSpot Cloud 26.8.0.cl +
*Upgrade notes*: See link:#_breaking_changes[Breaking changes] below before upgrading. +
*Recommended SDK versions*: Visual Embed SDK v1.51.0 and later

[#_breaking_changes]
[WARNING]
====
*26.8.0.cl contains three breaking changes for embedded applications:*

. *SDK flag defaults changed* — `hideIrrelevantChipsInFilterPanel`, `compactHeader`, `coverFilterPage`, and `aether` are now enabled (`true`) by default. Set them explicitly to `false` to preserve previous behavior.
. *Home page and navigation V1/V2 removed* — All embedded sessions now render the V3 experience. `HomePage.V1`, `HomePage.V2`, and the equivalent navigation flags are ignored by the platform. Migrate your `AppViewConfig` before upgrading.
. *Granular download privileges* — The *Can Download Data* privilege is split into *Can Download Visuals* (PDF/PNG) and *Can Download Detailed Data* (XLSX/CSV). Update any programmatic privilege assignments before upgrading.
====

[.cl-table, cols="2,4", frame=none, grid=none]
|=====
a|
[.cl-label]
*Version 26.8.0.cl*

a|
// SOURCE: SCAL-295712
[discrete]
==== SDK flag defaults updated [.version-badge.breaking]#Breaking#

The following SDK flags are now enabled (`true`) by default for all embedded ThoughtSpot applications in Visual Embed SDK v1.51.0. If your application explicitly passes `false` for any of these flags, that value is still honored. If you relied on the previous default of `false`, you must now explicitly set the flag to `false` to preserve that behavior.

[cols="2,4", options="header"]
|===
| Flag | Description
| `hideIrrelevantChipsInFilterPanel` | Hides filter chips that are not relevant to the currently displayed visualization. Previously disabled by default.
| `compactHeader` | Enables the compact Liveboard header layout. Previously disabled by default.
| `coverFilterPage` | Applies a cover layout to the Liveboard filter page. Previously disabled by default.
| `aether` | Enables the Aether visual design system for embedded components. Previously disabled by default.
|===

// TODO: verify exact TypeScript flag names against visual-embed-sdk v1.51.0 release tag (SCAL-295712)

---

// SOURCE: SCAL-312731
[discrete]
==== Home page and navigation V1/V2 removed [.version-badge.breaking]#Breaking#

In 26.8.0.cl, the classic (V1) and V2 home page and navigation experiences are removed from all ThoughtSpot embedded applications. All embedded sessions render the V3 navigation and home page experience regardless of the SDK configuration passed at initialization.

If your application uses any of the following, update your configuration before upgrading:

* `homePage: HomePage.V1` or `homePage: HomePage.V2` in `AppViewConfig`
* Navigation flags that previously enabled V1 or V2 navigation

*Migration steps:*

. Update `AppViewConfig` to use `discoveryExperience` with `primaryNavbarVersion: PrimaryNavbarVersion.Sliding` and `homePage: HomePage.ModularWithStylingChanges` for the V3 experience, or `homePage: HomePage.Focused` for the V4 focused home page.
. Remove any conditional logic in your codebase that branches on `HomePage.V1` or `HomePage.V2`.
. Review and test any CSS customizations targeting V1 or V2 navigation selectors.

For migration details, see xref:full-app-customize.adoc[Customize full application embedding] and xref:customize-homepage-full-embed.adoc[Customize home page experience].

---

// SOURCE: SCAL-309481
[discrete]
==== Liveboard PDF export — continuous layout generally available [.version-badge.new]#New#

The WYSIWYG Liveboard PDF export experience (`isContinuousLiveboardPDFEnabled`) is now generally available. This feature was in Beta since 26.5.0.cl (SDK v1.48.x). The Beta label has been removed and this parameter is recommended for production use.

When `isContinuousLiveboardPDFEnabled` is set to `true`, each Liveboard tab is rendered on a single continuous page that matches the UI layout. Setting this to `false` retains the paginated A4 format.

// TODO: Confirm with engineering whether isContinuousLiveboardPDFEnabled becomes true by default in 26.8.0.cl or remains opt-in (SCAL-309481)

For more information, see xref:embed-pinboard.adoc#_download_a_liveboard[Download a Liveboard].

---

// SOURCE: SCAL-307283
[discrete]
==== Personalized Views — TML portability [.version-badge.new]#New#

Personalized Views in Liveboards can now be reliably ported across ThoughtSpot environments using TML. The following changes apply to Liveboard TML and the Import/Export TML APIs:

New TML fields:::
* `owned_by` — Assigns the owner of each Personalized View by `username` or email address. Preserves ownership when a Liveboard is promoted between clusters or Orgs.
* `obj_ID` — A stable identifier for each view across environments, enabling reliable merge behavior on import.

Smart merge on import:::
When importing a Liveboard TML containing Personalized Views, ThoughtSpot now:

* Preserves views that exist only in the destination environment.
* Appends views that exist only in the incoming TML.
* Updates views present in both the TML and the destination environment.

The previous behavior overwrote all destination views on import. This change ensures end-user Personalized Views are not deleted during Liveboard promotions.

// TODO: Verify exact TML field names (owned_by vs owner, obj_ID casing) with engineering (SCAL-307283)

---

// SOURCE: SCAL-273333
[discrete]
==== Granular download privileges [.version-badge.breaking]#Breaking#

The *Can Download Data* privilege has been split into two separate privileges that provide more granular control over download permissions:

[cols="2,3", options="header"]
|===
| New privilege | Grants access to
| *Can Download Visuals* | PDF and PNG format downloads of Liveboards and visualizations
| *Can Download Detailed Data* | XLSX and CSV format downloads of Liveboards and visualizations
|===

*Impact on embedded applications:*

* Users who previously held the *Can Download Data* privilege are automatically migrated. Access is preserved without action.
* If your application makes programmatic privilege assignments via REST API or TML-defined role definitions, update those assignments to use the new granular privilege names before upgrading.
* If your application gates the visibility of download `Action` IDs based on privilege checks, validate your logic against the new privilege structure.

---

// SOURCE: SCAL-310399
[discrete]
==== Spotter Analysts (Spots) [earlyAccess eaBackground]#Early Access#

ThoughtSpot introduces Spotter Analysts, also referred to as *Spots*, which are custom AI analysts scoped to a defined data domain. Administrators can create, configure, and manage Spots, define their data scope and behavioral instructions, and surface them to end users in the Spotter interface.

// TODO: Verify whether Spots are accessible via SpotterEmbed or a new embed component, and confirm any new SDK events or flags required for embedding (SCAL-310399)

---

// SOURCE: SCAL-309017
[discrete]
==== SpotterViz Insight Tiles [earlyAccess eaBackground]#Early Access#

Users can now pin AI-generated insights from SpotterViz as tiles directly on a Liveboard. Insight Tiles are available within the SpotterViz interface on embedded Liveboards.

For more information, see xref:embed-spotterViz.adoc[SpotterViz in embedded Liveboards].

// TODO: Verify whether new EmbedEvent or HostEvent identifiers are introduced for Insight Tile interactions, and document them in xref:embed-spotterViz.adoc (SCAL-309017)

---

// SOURCE: SCAL-306340
[discrete]
==== Spotter Model now generally available [.version-badge.new]#New#

The Spotter Model is now generally available. Administrators can configure the Spotter Model to control how Spotter interprets data, including column relationships, synonyms, and preferred aggregations. The Early Access label has been removed.

// TODO: Confirm whether Spotter Model configuration is available via REST API v2 and link to the relevant API reference page (SCAL-306340)

---

// SOURCE: SCAL-293483
[discrete]
==== Spotter onboarding for new users [.version-badge.new]#New#

ThoughtSpot now provides a guided first-run onboarding experience for Spotter. When users open Spotter for the first time, they are guided through the core features and workflows of the Spotter interface. This feature is enabled by default for new users on ThoughtSpot Cloud.

// TODO: Confirm whether embedded Spotter (SpotterEmbed) surfaces the onboarding flow, and whether the onboarding experience can be suppressed using an SDK flag (SCAL-293483)

---

// SOURCE: SCAL-313088
[discrete]
==== Business Terms auto-generation deprecated [.version-badge.deprecated]#Deprecated#

Automatic Business Term generation is deprecated in 26.8.0.cl. The Business Terms workflow is now used to allow administrators to manually add descriptive context to columns and data objects, replacing the automated generation flow.

*Impact:* Workflows or integrations that rely on scheduled Business Term auto-generation will no longer execute.

// TODO: Confirm whether any REST API endpoints for Business Terms auto-generation are deprecated or removed in 26.8.0.cl (SCAL-313088)

---

// SOURCE: SCAL-298371
[discrete]
==== Timezone-aware dates now generally available [.version-badge.new]#New#

Timezone-aware date handling is now generally available. Date values in search queries and results now reflect the configured user timezone, ensuring consistent date interpretation across time zones. This feature was previously in Beta.

---

// SOURCE: SCAL-301050
[discrete]
==== Column Alias UDF in Autocomplete now generally available [.version-badge.new]#New#

Column aliases defined by users are now consistently recognized and surfaced in Autocomplete suggestions across Search and Spotter. This feature was previously in Early Access.

---

// SOURCE: SCAL-273513
[discrete]
==== Connection new experience now generally available [.version-badge.new]#New#

The redesigned Embrace connection setup experience is now generally available. The refreshed UI for creating and managing data connections includes improved guidance and field validation. The Early Access label has been removed.

---

[discrete]
==== Visual Embed SDK

The Visual Embed SDK version 1.51.0 includes the breaking changes and new features listed above.
For the full SDK changelog, see xref:api-changelog.adoc[Visual Embed changelog].

---

[discrete]
==== REST API v2

For information about REST API v2 enhancements in this release, see the xref:rest-apiv2-changelog.adoc[REST API v2.0 changelog].

---

|=====

== July 2026

**Release version**: ThoughtSpot Cloud 26.7.0.cl +
*Upgrade notes*: No breaking changes +
*Recommended SDK versions*: Visual Embed SDK v1.50.0 and later

[.cl-table, cols="2,4", frame=none, grid=none]
|=====
a|
[.cl-label]
*Version 26.7.0.cl*

a|
[discrete]
==== SpotterViz for Liveboards [earlyAccess eaBackground]#Early Access#
You can now use SpotterViz in your embedding application to help your users build and edit Liveboards through a conversational interface. Instead of manually configuring charts and layouts, your users can describe what they want and SpotterViz generates the Liveboard for them, including the new tabs, chart types, data filters, and scheduled deliveries.

For SpotterViz customization in embedded view, the Visual Embed SDK also provides several options to customize the SpotterViz panel experience. For more information, see xref:embed-spotterViz.adoc[SpotterViz in embedded Liveboards].

---

[discrete]
==== Spotter embedding

Spotter file upload in embedded apps::
Applications embedding the Spotter interface can now allow their users to xref:embed-spotter.adoc#_enable_file_upload_in_spotter_chat[upload files directly in the Spotter chat panel].

Spotter conversation history::
You can now save your Spotter conversation and manage chat history using Spotter AI REST APIs. For more information, see xref:spotter-agent-conversation-mgmt-apis.adoc[APIs for managing saved conversations].

Spotter Agent instructions::
You can configure and retrieve behavioral instructions for the Spotter agent using REST APIs. For more information, see xref:spotter-agent-instructions.adoc[Spotter AI agent instructions APIs].

---

[discrete]
==== Focused home page experience [earlyAccess eaBackground]#Early Access#

In full application embedding with the V3 navigation and home page experience, ThoughtSpot provides an additional option to switch to the V4 focused home page experience. The focused home page experience provides a streamlined, contemporary experience along with the Spotter panel. For more information, see xref:full-app-customize.adoc[Customize full application embedding].

---

[discrete]
==== Webhooks enhancements

ThoughtSpot introduces the following features and enhancements for webhook configuration and management:

* New Webhooks page in the UI [earlyAccess eaBackground]#Early Access# +
The *Develop* page now includes a xref:webhooks-ux.adoc[dedicated *Webhooks* page] for creating, managing, and monitoring webhooks within the Org context.
* Storage configuration retrieval +
The `GET /api/rest/2.0/webhooks/storage-config` REST API endpoint to xref:webhooks-api.adoc#_retrieving_storage_information_for_webhook_configuration[get storage configuration details].
* GCS storage configuration for webhook delivery +
Administrators can now xref:webhooks-gcs-storage.adoc[configure Google Cloud Storage (GCS) buckets as a storage destination] for webhook payload delivery on GCP-hosted ThoughtSpot clusters.
* Webhook activation and deactivation +
You can enable or disable a webhook connection in the UI or through REST API.
* Selective configuration reset +
The xref:webhooks-api.adoc#_updating_a_webhook[webhook update API endpoint] supports the `reset_options` parameter to remove specific optional configuration sections without replacing the full webhook configuration.

---

[discrete]
==== SpotterCode Agent in Visual Embed Playground [earlyAccess eaBackground]#Early Access#

The Visual Embed SDK Playground now includes SpotterCode Agent, an AI-powered coding assistant. The SpotterCode panel displays pre-built prompts relevant to the component you are embedding, a prompt interface for user queries, and generates embed code. It generates boilerplate code automatically, accelerates building code and iterating embed configurations.

For more information, see xref:developer-playground.adoc#spottercode-panel[Using SpotterCode in the Playground].

---

[discrete]
==== Org isolation for per-org SAML and OIDC authentication
ThoughtSpot now enforces strict org isolation when users authenticate through a per-org identity provider (IdP). When a per-org IdP sends SAML or OIDC group claims that reference Orgs outside its authorized scope, ThoughtSpot silently drops those claims and records them as security audit events. This prevents a rogue IdP administrator in one Org from using group assertions to gain unauthorized access to another Org. Manually-assigned existing Org memberships are unaffected.

---

[discrete]
==== Visual Embed SDK
The Visual Embed SDK version 1.50.0 includes several new features and enhancements. For more information, see the xref:api-changelog.adoc[Visual Embed changelog].

---

[discrete]
==== REST API v2
For information about REST API v2 enhancements in this release, see the xref:rest-apiv2-changelog.adoc[REST API v2.0 changelog].

---

|=====

== June 2026

**Release version**: ThoughtSpot Cloud 26.6.0.cl +
*Upgrade notes*: No breaking changes. +
*Recommended SDK versions*: Visual Embed SDK v1.49.0 and later

[.cl-table, cols="2,4", frame=none, grid=none]
|=====
a|
[.cl-label]
*Version 26.6.0.cl*

a|
[discrete]
==== Chart and table overrides [.version-badge.new]#New#
You can now apply visualization overrides to charts and tables generated from a search query in ThoughtSpot search and full application embedding. The `visualOverrides` property in `SearchViewConfig` and `AppViewConfig` allows developers to apply at the embed initialization time:

* Chart overrides +
Control legend visibility and position, data label display and per-column filter
thresholds, regression lines, grid lines, axis range and label settings, series
colors, and conditional formatting rules including font and background styling.
* Table overrides +
Control column visibility, text wrapping, row height and padding density, table
theme, and column summary visibility with per-column exceptions.

For more information, see xref:viz-overrides.adoc[Configuring visualization overrides].

---

[discrete]
==== Spotter AI and embedding enhancements [.version-badge.new]#New#

This release introduces the following enhancements for Spotter AI workflows and embedded Spotter applications.

* Spotter embedding: +
Spotter now includes data literacy skills that help users understand the underlying data model. Users can ask Spotter to explain available data sources, fields, and relationships in plain language within a conversation session.
* Spotter AI API enhancements: +
** The `POST /api/rest/2.0/ai/answer/create` endpoint now supports a `session_identifier` parameter to maintain conversation context across requests.
** The `POST /api/rest/2.0/ai/conversation/converse` endpoint now supports file attachments via a `file_identifiers` parameter.

---

[discrete]
==== REST API v2
For information about REST API v2 enhancements in this release, see the xref:rest-apiv2-changelog.adoc[REST API v2.0 changelog].

---

|=====
