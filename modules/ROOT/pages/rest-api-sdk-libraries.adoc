= SDK libraries
:toc: true
:toclevels: 1

:page-title: SDK libraries
:page-pageid: rest-api-sdk-libraries
:page-description: ThoughtSpot provides SDK libraries that allow you to integrate ThoughtSpot REST APIs in your application.

ThoughtSpot provides official SDK libraries for integrating ThoughtSpot REST API v2.0 into your applications.

== Python SDK for REST API v2.0 [version newBackground]^New in jul.26.mt^

// SOURCE: SCAL-323283, SCAL-216861
// STATUS: ⚠️ GATE — Do not publish until the Python SDK is released on PyPI and SCAL-216861 is confirmed shipped.
// TODO: [WRITER] Confirm the PyPI package name with the SDK team before publishing.
// TODO: [WRITER] Add the GitHub repository URL for the Python SDK once available.
// TODO: [WRITER] Confirm the minimum supported Python version with the SDK team.
// TODO: [WRITER] Update the xref below to match the final published page name for the Python SDK reference.

ThoughtSpot provides an official Python SDK for REST API v2.0, enabling Python developers to integrate ThoughtSpot data operations, analytics, and AI workflows directly into Python applications without writing raw HTTP requests.

[NOTE]
====
The Python SDK is available as part of the ThoughtSpot Cloud jul.26.mt release and is published on PyPI.
// TODO: [WRITER] Insert the confirmed PyPI package name and link here before publishing.
====

Install the SDK using pip:

[source,bash]
----
# TODO: [WRITER] Replace with the confirmed PyPI package name
pip install thoughtspot-rest-api-sdk
----

For the full SDK reference, authentication setup, and code examples, see xref:python-sdk.adoc[Python SDK for REST API v2.0].

== Community SDKs

The following community-maintained SDK libraries are available for ThoughtSpot REST API integration. These SDKs are not officially supported by ThoughtSpot.

== Additional resources

* xref:rest-api-v2-reference.adoc[REST API v2.0 reference]
* xref:rest-apiv2-changelog.adoc[REST API v2.0 changelog]
